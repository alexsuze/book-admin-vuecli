import { createStore } from 'vuex'
import Sidebar from '@/store/SideBar';

export default createStore({
  state: {
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    Sidebar
  }
})
